apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demo-worker
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: demo-worker
  namespaceSelector:
    matchNames:
      - demo
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
